package:
  name: diffusers
  version: 0.4.20.dev0

source:
   git_rev: 
   git_url: https://github.com/catid/unfiltered-diffusers.git

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  imports:
    -

about:
  home:
